server:
  port: 8000
spring:
  application:
    name: product-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        # enabled 默认为 true，可省略
        # group 默认为 DEFAULT_GROUP，也可省略